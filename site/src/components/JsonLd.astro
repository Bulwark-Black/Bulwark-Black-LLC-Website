---
interface Props {
  data: unknown;
}
const { data } = Astro.props;
const payload = JSON.stringify(data).replace(/</g, "\\u003c");
---
<script type="application/ld+json" set:html={payload} is:inline />
